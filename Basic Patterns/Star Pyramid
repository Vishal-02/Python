n = int(input("Enter the number of rows: "))
m = n
for x in range(1, n+1):
    for k in range(m-1):
        print(end = " ")
    m-=1
    print("* " * x)
    '''
    The only difference between this code and the inverted triangle code 
    is that the last line of code has a space included after the star
    which actually makes it look like a pyramid
    '''
